# Preview comparison

Body text long enough to wrap two or three times, so line height and the measure are
both visible at once. It carries **bold**, *italic*, `inline code` and a
[link](https://example.com) so their weight and colour can be compared in place.

## Heading two

- Bulleted item
- A longer item whose text runs past the end of the line, so the wrap and the hanging
  indent under the marker are both visible
  - Nested item

1. Ordered item
2. Second ordered item

- [ ] Unticked task
- [x] Ticked task

> A blockquote that wraps once, so the bar's height against its text can be judged.

### Heading three

| Column | Count | Notes |
|---|---|---|
| first | 42 | short |
| a longer value | 7 | wraps if the column is tight |

```powershell
Get-ChildItem -Path $env:TEMP | Where-Object { $_.Length -gt 1MB }
```

```json
{ "key": "value", "count": 42 }
```

---

Closing paragraph after a thematic break, to show the space a rule leaves on each side.
