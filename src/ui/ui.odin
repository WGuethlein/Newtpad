// Layer: ui — immediate-mode UI built each frame from input + document state.
// Fully handmade; no frameworks. Produces draw intent for the renderer and
// queues events to the frame arena. Never calls Win32/COM directly.
//
// Empty for now; arrives after the renderer can draw text.
package ui
